<UserControl x:Class="HS.Views.Pages.RoomsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:entities="clr-namespace:HS.Context.Entities"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <UniformGrid DockPanel.Dock="Bottom">
            <Button Command="{Binding CreateNewRoomTypeCommand}">
                <Button.Content>
                    <TextBlock Text="Add room type"></TextBlock>
                </Button.Content>
            </Button>
            <Button>
                <Button.Content>
                    <TextBlock Text="Delete"></TextBlock>
                </Button.Content>
            </Button>
        </UniformGrid>
        <ListBox ItemsSource="{Binding Rooms, UpdateSourceTrigger=PropertyChanged}">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="entities:Room">
                    <TextBlock Text="{Binding Number}"></TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
    
</UserControl>
