<UserControl x:Class="HS.Views.Pages.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HS.Views.Pages"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding Count}" Foreground="WhiteSmoke"></TextBlock>
            <DatePicker materialDesign:HintAssist.Hint="низ" Foreground="GhostWhite" Width="170"
                        HorizontalContentAlignment="Center" FontSize="15"
                        SelectedDate="{Binding LowerBound}"></DatePicker> 
            <DatePicker materialDesign:HintAssist.Hint="верх" Foreground="GhostWhite" Width="170"
                        HorizontalContentAlignment="Center" FontSize="15"
                        SelectedDate="{Binding UpperBound}"></DatePicker> 
        </StackPanel>
        
    </Grid>
</UserControl>
