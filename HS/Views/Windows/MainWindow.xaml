<Window x:Class="Hotel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        DataContext="{Binding MainViewModel, Source={StaticResource Locator}}"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.InputBindings>
        <KeyBinding Modifiers="Control" Key="Q" 
                    Command="{StaticResource CloseAppCommand}"/>
    </Window.InputBindings>
    <DockPanel LastChildFill="False">
        <Grid>
            <Grid.Resources>
                <ResourceDictionary.MergedDictionaries>
                    <ResourceDictionary Source="../../Templates/ViewModelsDataTemplates.xaml"></ResourceDictionary>
                </ResourceDictionary.MergedDictionaries>
            </Grid.Resources>
            <ContentControl Content="{Binding CurrentViewModel}"></ContentControl>
        </Grid>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>Добавлен новый клиент</StatusBarItem>
        </StatusBar>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Center">
            <RadioButton IsChecked="True" Command="{Binding ShowReservationsViewCommand}" Content="Reservations"></RadioButton>
            <RadioButton Command="{Binding ShowClientsViewCommand}" Content="Clients"></RadioButton>
        </StackPanel>
    </DockPanel>
</Window>
